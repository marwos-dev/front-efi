import React from 'react';
/* import NavbarMovile from './'; */

const Layout = (props) => {
    return (
        <>
       {/*  <NavbarMovile/>  */}
        {props.children}
        </>
    );
}

export default Layout;